SET NOCOUNT ON;
SELECT N'No se requieren datos semilla.' AS Resultado;
SELECT N'Los tipos de comunicación (0-5) y persistencia (0-3) son contratos de aplicación; no se agrega una tabla paramétrica hasta que el monitor administrativo la requiera.' AS Detalle;
